class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        # s_list=list(s)
        # t_list=list(t)


        if sorted(s)==sorted(t):
            return True
        else:
            return False
    # Also other way is to use counter from collections
    #     counter(s)==counter(t)
        
